import type { WardMigrationRow } from "../../src/lib/civic-geo/types";

/**
 * Ward-level migration rows — add only from GO / gazette / official tables.
 * Do NOT add rows derived from visual media maps.
 */
export const WARD_MIGRATION_SEED: WardMigrationRow[] = [];
